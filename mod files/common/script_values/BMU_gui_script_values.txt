BMU_stakeholder_count = {
	# every_in_list = {
	# 	variable = stakeholders_gui
	# 	limit = {
	# 		story_owner = {
	# 			highest_held_title_tier >= tier_county
	# 			any_pressed_claim = { count = 0 }
	# 			any_contender_story = { count = 0 }
	# 		}
	# 	}
	# 	add = 1
	# }
}

BMU_supporter_gambit_cost = {
	add = scope:recipient.highest_held_title_tier
	if = {
		limit = {
			NOR = {
				scope:recipient = { any_supporter_story = { var:pressed_claim_story = scope:pressed_claim }}
				scope:actor = { BMU_target_is_relevant_in_pressed_claim_v2 = { TARGET = scope:recipient PRESSED_CLAIM = scope:pressed_claim }}
			}
		}
		multiply = 2
	}
	multiply = 10
}

BMU_gambits_cost_check_gui = {
	scope:actor = {
		ordered_owned_story = {
			limit = {
				story_type = BMU_contender_story
				var:pressed_claim_story = scope:pressed_claim
			}
			add = BMU_gambit_remaining
		}
	}
	subtract = BMU_supporter_gambit_cost
}